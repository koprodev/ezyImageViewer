<?xml version="1.0" encoding="utf-8"?>
<!-- MSIX identity manifest (Q7=b). pack-msix.ps1 writes the version and publisher attributes;
     the publisher must exactly match the signing certificate subject. -->
<Package
  xmlns="http://schemas.microsoft.com/appx/manifest/foundation/windows10"
  xmlns:uap="http://schemas.microsoft.com/appx/manifest/uap/windows10"
  xmlns:rescap="http://schemas.microsoft.com/appx/manifest/foundation/windows10/restrictedcapabilities">

  <Identity Name="GRTech.ezyImageViewer"
            Publisher="{{PUBLISHER}}"
            Version="{{VERSION}}"
            ProcessorArchitecture="x64" />

  <Properties>
    <DisplayName>ezy Image Viewer</DisplayName>
    <PublisherDisplayName>grtech-devpro</PublisherDisplayName>
    <Logo>Assets\StoreLogo.png</Logo>
  </Properties>

  <Dependencies>
    <TargetDeviceFamily Name="Windows.Desktop" MinVersion="10.0.19041.0" MaxVersionTested="10.0.26100.0" />
    <PackageDependency Name="GRTech.ezyImageViewer.CodecHost"
                       Publisher="{{PUBLISHER}}"
                       MinVersion="{{CODEC_HOST_VERSION}}" />
  </Dependencies>

  <Resources>
    <Resource Language="ko-KR" />
  </Resources>

  <Applications>
    <Application Id="App" Executable="ezyImageViewer.exe" EntryPoint="Windows.FullTrustApplication">
      <uap:VisualElements DisplayName="ezy Image Viewer"
                          Description="ezy Image Viewer"
                          BackgroundColor="transparent"
                          Square150x150Logo="Assets\Square150x150Logo.png"
                          Square44x44Logo="Assets\Square44x44Logo.png" />
      <Extensions>
        <!-- Snipping Tool redirect-uri callback (FR-CAP-001): the scheme must match
             SnipProtocol.Scheme. -->
        <uap:Extension Category="windows.protocol">
          <uap:Protocol Name="ezyimageviewer" DesiredView="default">
            <uap:DisplayName>ezy Image Viewer</uap:DisplayName>
          </uap:Protocol>
        </uap:Extension>
      </Extensions>
    </Application>
  </Applications>

  <Capabilities>
    <rescap:Capability Name="runFullTrust" />
  </Capabilities>
</Package>
